name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v1
    - name: make
      env: 
        CC: gcc
      run: make
    - name: make test
      env: 
        CC: gcc
      run: make test
