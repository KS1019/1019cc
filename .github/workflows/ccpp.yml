name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      CC: "gcc"
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make
    - name: make test
      run: make test
