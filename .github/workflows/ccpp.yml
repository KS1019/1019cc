name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@master
      with:
          fetch-depth: 1
          path: 1019cc
    - name: cd
      run: cd 1019cc/
    - name: make test
      run: make test
