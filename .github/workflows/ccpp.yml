name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@master
      with:
          fetch-depth: 1
    - name: configure
      run: cd ~
    - name: make test
      run: make test
    - name: make clean
      run: make clean
