name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on:
      using: 'docker'
      image: 'Dockerfile'
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make
    - name: make test
      run: make test
